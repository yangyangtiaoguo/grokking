# Grokkability 选题：现状说明与核查记录

> 记录日期：2026-08-20。本文件客观记录对合作者提案《Weight Decay as a Description-Length Field / The Grokkability Phase Diagram of Succinct Circuits》的核查结果、已做的重构决策、已完成的理论推导，以及尚未解决的开放问题。供后续讨论与选题定稿使用。

---

## 1. 提案原貌（一句话）

证明并测量：weight decay 是作用在 Transformer 电路上的 MDL 原理（H1）；grokking 是该"描述长度场"触发的 spinodal 坍塌（H2）；在 (正则 λ, 数据偏斜 α) 平面上计算决定"能否 grok"的临界线 λ_c(α)（H3）；并给出拯救被困模型的最小平衡课程剂量 m\*(α,λ)（H4）。

奠基文献：Bergsträßer 等《Transformers are Inherently Succinct》(ICLR 2026)；Yizheng Zhao《Do Transformers Grok Succinct Algorithms?》(ACL 2026 Findings)；两者均已核实真实。

---

## 2. 引用核查结果（2026-08-20，经 arXiv API + ACL Anthology + 网络检索）

### 2.1 确认真实且吻合
- arXiv:2510.19315 — *Transformers are Inherently Succinct* — Bergsträßer, Cotterell, Lin。奠基 A。
- ACL 2026 Findings 1301 — *Do Transformers Grok Succinct Algorithms?* — **Yizheng Zhao**（引用请写全名）。奠基 B。
- arXiv:2509.22445 — *Bridging Kolmogorov Complexity and Deep Learning* — Shaw, Cohan, Eisenstein, Toutanova (ICLR 2026)。已构造 Gaussian-mixture 变分 MDL 码，并指出"优化器从随机初始化找不到低复杂度解"。**使 M1(b) 变分编码器无法作为独立方法学贡献。**
- arXiv:2603.24746 — *Grokking as a Falsifiable Finite-Size Transition* — Bi 等。用群阶 p（Z_p）作 extensive 变量 + Binder crossings（注意：不是 (f,λ) 相图）。
- arXiv:2604.04655 — *Grokking as Dimensional Phase Transition* — Ping Wang。
- arXiv:2511.04760 — *When Data Falls Short* — Singh, Belilovsky, Aljundi。用 KD 拯救、按数据量（非偏斜）、无解析式。

### 2.2 幻觉引用（必须删除）
- **arXiv:2603.00384**：原分析称其为"*Mapping Delayed Generalization*，按数据量画相图"。**真实论文是一篇老年人语音隐私的 HCI 论文**（Cuadra 等, CHI）。原意应替换为真实的按数据量相图工作：**arXiv:2205.10343**（Liu 等《Effective Theory》）+ **arXiv:2210.01117**（Omnigrok）。

### 2.3 原分析漏掉的两处撞车（威胁防线）
- **arXiv:2602.08857** — *Discovering Interpretable Algorithms by Decompiling Transformers to RASP* — Huang, Bakalova, Bhattamishra, Merrill, Hahn。**已实现 Transformer→RASP 反编译**，因此 M1(a) RASP 编码器**不是干净退路**。应改为"应用其反编译器 + 我们的算子计数/长度度量"。
- **arXiv:2607.23967** — *Grokking on the Weight-Decay Clock* — Taeyoung Kim。推出 grokking 延迟时间标度 τ∼(1−β)/(ηλ)。威胁 H2 的 τ 标度，须以"时间轴 vs 终点轴"切割。

---

## 3. 重构决策

### 3.1 收敛为一篇（不预先承诺两篇）
- **论文 A（唯一要全力做的，目标 ICLR/NeurIPS 2027 theory track）**：偏斜相图。骨架 = H3 + H4 + 亚稳寿命 τ∝exp(ΔB) + T3/T4 + M4/M5(+M6)。贡献 C1（λ_c(α)）、C2（m\*）、C3（延迟 grok τ）、C4（Circuit-MDL 度量，仅作工具支撑）。
- **meter（M1）切不出去**：λ_c=ΔD/**Δℓ** 需要 Δℓ 被测出，度量器天然在 A 内（以 C4 身份）。
- **A 不需要证明 H1**：A 只需一个弱 MDL ansatz（自由能 F=D+λℓ），其成立与否由 A 自己的相图能否零参数预测边界来间接检验。

> **⚠ 上句已修正（2026-08-20）**：见第 6 节。"A 不依赖 H1"讲过头了——弱 ansatz 的 λℓ 项本身就是 H1。准确表述是"A 只依赖 H1-弱（单调性），核心形状预测能绕过 H1 走 ΔD(α)"。
- **H1′/H2 = A 的附录或机会性衍生**，实验数据出来后再决定是否抽成独立"论文 B"。B 的现实归宿是 interpretability workshop 或 A 的一节，**不按独立顶会规划**（被 2509.22445 / 2602.08857 / 2607.23967 压制）。写作串行，不并行。

### 3.2 各假设定位
| 假设 | 定位 | 理由 |
|---|---|---|
| H3 λ_c(α) | **主贡献 C1** | 唯一无近作压制的空白 |
| H4 m\*(α,λ) | **主贡献 C2** | 空白，直接回应 Zhao 开放局限 |
| 亚稳 τ∝exp(ΔB) | **主贡献 C3** | H2 里唯一救得活的部分（Kramers 逃逸，非临界指数）|
| H1 范数↔长度 | 降级；改等范数横截面检验 H1′ | 原 M2 轨迹 Spearman 是共线性，测不出机制 |
| H2 ν=1/2 临界指数 | **放弃** | 种子噪声下不可证伪；且撞四篇相变论文 |
| H2 滞回 | 保留为探索性观察 | 二值方向明确，噪声下可测 |
| T1 玩具定理 | 降为验证性 lemma | 证明范式已被 ICML 2026 ridge 占位 |

---

## 4. 已完成的理论推导（T3 / T4）

### 4.1 锁定定义
- 任务：n-bit 二进制计数器。进位链长度 c(x) = x 末尾连续 1 的个数（只由低位决定）。
- 偏斜主定义：magnitude-Zipf，Pα(x) ∝ (x+1)^(−α)。

### 4.2 修正提案两处不精确
1. **Benford 对进位链几乎无效**：Benford 是高位定律，进位链只看低位。主线只用 magnitude-Zipf；Benford 应作负对照（预测它**不**移动临界线）。
2. **"Zipf 指数抑制长进位链"只在 α>1 成立**：α<1 几乎无抑制。由此得到 λ_c(α) 在 **α=1 处有 kink**——比平滑曲线更强、更可证伪（flat for α<1, kink at α=1, rising for α>1）。

### 4.3 T3 结果（Lemma A + B → λ_c(α)）
- Lemma A（闭式）：Pr_α(c≥k) = 2^(−kα)·Σ_{j<2^{n−k}}(j+1)^(−α) / Σ_{x<2^n}(x+1)^(−α)。α>1 时衰减率 →2^(−α)。
- Lemma B：ΔD(α) ∝ N(E₀[c]−E_α[c])。
- 推论：λ_c(α)=ΔD(α)/Δℓ，α<1 平坦、α=1 拐折、α>1 上翘。
- 零成本校验：蒙特卡洛采样直接验证 c 分布，不需训练模型。

### 4.4 T4 结果（拯救剂量，钉死 d₀）
- **d₀ := E₀[c]−E_α[c]**（每个平衡样本的进位链 surprise）。
- m\*(α,λ)=Δℓ(λ_c(α)−λ)/d₀；代入 T3 得干净形式 **m\*=N − Δℓ·λ/(E₀[c]−E_α[c])**。
- 极限：λ→0 ⟹ m\*→N；λ→λ_c ⟹ m\*→0。
- **新预测**：偏斜越狠 d₀ 越大、单样本越值钱；**最优课程注入最长进位链（最稀有）而非均匀平衡**。
- 可证伪 P4：m vs (λ_c−λ) 线性、斜率 1/d₀（零自由参数预测，非拟合）。

---

## 5. 尚未解决的开放问题（下一步探索清单）

1. **Δℓ 是否真的可被两个 meter 一致测出？** C1–C4 全挂在 Δℓ 上。若 RASP 计数与变分码给出的 Δℓ 差一个数量级，λ_c(α) 的绝对标度就对不齐。需先在合成电路上标定。
2. **弱 MDL ansatz（F=D+λℓ）本身是否成立？** 这是 A 的隐藏地基。若优化动力学不服从这个自由能，整条临界线推导失效。
3. **α=1 kink 是否在有限 n、有限步数预算下可观测？** 理论 kink 可能被有限尺寸效应抹平。
4. **"carry-balanced" 的精确操作定义？** T4 的最优课程是"注入最长进位链"，但如何在训练管线里高效构造这些样本、d₀ 如何实测，需落地。
5. **C_mem / C_alg 两基态的自由能模型是否过度简化？** 真实 loss landscape 未必是干净双阱。
6. **泛化性（M6）**：λ_c(α) 的相图结构能否在 Dyck-1 / 模加上复现？若只在计数任务成立，洞察的可援引性打折。
7. **待补引用核查（2026-08-21 补齐，arXiv API + DBLP）**：
   - Omnigrok = arXiv:2210.01117 "Omnigrok: Grokking Beyond Algorithmic Data"（Liu et al., 2022）✓
   - Žunkovič-Ilievski = arXiv:2210.15435 "Grokking phase transitions in learning local rules with gradient descent"（2022 arXiv；提案写"2024"疑指期刊版）✓
   - Sharkey 2024：**arXiv/DBLP 均查无此文**（所有 Sharkey 名下无 grokking/Pareto 相关论文）。最接近的真文是 Braun, Bushnaq, Heimersheim, Mendel & **Lee Sharkey**, "Interpretability in Parameter Space: Minimizing Mechanistic Description Length with Attribution-based Parameter Decomposition", arXiv:2501.14926（2025）。提案该引用应视为不可核实（疑为幻觉/混淆），引用时改为 2501.14926 并重新核对它是否真的支持"描述长度=电路 Pareto 前沿一维"的表述。
   - Hinton & van Camp 1993："Keeping neural networks simple by minimizing the description length of the weights"，NeurIPS 1993 proceedings——早于 arXiv CS 部分，**无 arXiv 号**，按会议论文集引用即可。

---

## 6. 弱 ansatz 与 H1 的循环依赖：拆解结论（2026-08-20）

**问题**：A 想甩掉 H1，但弱 ansatz 里的正则项是 λ·ℓ（电路描述长度），而实际训练惩罚的是 λ·‖θ‖²。把 ‖θ‖² 换成 ℓ 这一步**本身就是 H1**。故"A 不依赖 H1"字面为假——存在循环依赖。

**拆解**：H1 分三档，A 只需最弱的一档。
- **H1-弱**：存在单调递增 g 使 ℓ≈g(‖θ‖₂)（范数越小⇒长度越小）。**A 只需这个。**
- H1-中：g 近似线性、a,b 可测。A 不需要。
- H1-强：ρ>0.95 + 机制解释（原提案）。A 不需要，是"论文 B"的野心。

**循环被两条独立路径打断**：
1. **T1 独立提供 H1-弱**：T1 玩具定理（哪怕只做 n≤6 穷举）证的正是"最小范数解=短电路"的单调性。T1→H1-弱→ansatz→A 是**单向链，非环**。证单调性远比证 ρ>0.95 容易。
2. **核心主张绕过 H1**：λ_c(α)=ΔD(α)/Δℓ 中，**分子 ΔD(α) 不含 ℓ**（纯进位链组合学，T3），决定临界线**形状**（平坦→α=1 kink→上翘）；ℓ（即 H1）只出现在分母 Δℓ，且不随 α 变，**只影响整体垂直标度**。故"α=1 kink + m\* 单调性"这一最鲁棒主张**完全不经过 H1**，只有"临界线绝对高度"才需要 H1-弱。

**依赖结构图**：
```
                         ┌─ 形状(kink/上翘) ── 只需 ΔD(α) ── T3组合学【硬，无H1】
   λ_c(α) = ΔD(α)/Δℓ ───┤
                         └─ 绝对标度 ──────── 需 Δℓ 即 ℓ ── 需 H1-弱 ← T1独立提供(单向)
```

**对选题的三个影响**：
1. **主叙事轴心 = 进位链组合学 ΔD(α)，不是 MDL/自由能**（后者是最弱一环，挂在它上面全垮）。MDL 只作为"解释 Δℓ 常数从哪来"的次要机制。
2. **T1 从门面升格为堵漏关键 lemma**——它是唯一能让 A 避免循环论证的东西，必须做（哪怕只 n≤6）。
3. **选题的不可辩驳内核（minimal viable claim）**：T3 组合学 → ΔD(α) 解析形状(含 α=1 kink) → 相图形状(M4) + 拯救相对标度 m\*∝(λ_c−λ)/d₀(T4/M5)。**这条链不经过 MDL、不经过 H1、不经过自由能物理**，纯组合学 + 记忆代价。立项以此为底线。

> **补充（2026-08-21）**：C2 的 m\* 用实测 λ_c 参数化后得 **m\*=κN(1−λ/λ_c(α))**，完全不含 Δℓ/H1/MDL，是最硬贡献。详见第 7 节先导协议与第 8 节。

---

## 7. 先导实验协议（立项前 Go/No-Go，总成本 ~8 GPU-h）

原则：不产论文结果，只在花 200 GPU-h 前证伪致命假设。每个先导都(a)不需跑完整 grokking，(b)对应明确否决判据，(c)总成本 <5% 正式实验。执行顺序 P0→P1→(P2‖P3)→P4。

| 先导 | 裁定 | 需跑到 grok | 成本 |
|---|---|---|---|
| **P0** | 组合学 ΔD(α)/d₀/α=1 kink（Lemma A/B）| 否，纯采样 | 0 GPU（CPU 几分钟）|
| **P1** | ② Δℓ(α) 平不平 → kink 稳不稳 | 否，只训到记忆 | ~2 GPU-h |
| **P2** | ③ 双阱是否成立 | 否，两已知解插值 | ~1 GPU-h |
| **P3** | ① 弱 ansatz 序关系 F_alg<F_mem | 否，复用 checkpoint | ~1 GPU-h |
| **P4** | 相图存在性 + λ_c 可测（2×2 角点）| 是，仅 4 格 | ~4 GPU-h |

**P0**（零算力，先做）：MC 采 Pα(x)∝(x+1)^(−α)，α∈{0,.5,1,1.5,2}，n∈{8,12,16}，统计 c(x)=trailing-ones。看 E_α[c] vs α 在 α=1 是否有 kink、Pr(c≥k) 在 α>1 是否→2^(−α)。❌无 kink → 重推 Lemma A 或改为渐近主张（最便宜否决点）。

> **P0 已执行（2026-08-21）= GO**。`experiments/p0_combinatorics.py` + `p0_plot.py`，图 `figures/p0_carry_stats.png`。结果：
> - **Lemma A 闭式 vs 精确枚举误差 5.55e-17**（机器精度）→ 组合学地基解析可靠。
> - **α>1 渐近 E_α[c]→1/(2^α−1) 精确**；尾部衰减率 α=2→0.250、α=1.5→0.354，即 2^(−α) 证实。
> - **d₀(α) 单调增**，α<0.75≈0、α=1 附近急升、α>1 快增 → 支撑 C2"偏斜越狠单样本越值钱"。
> - **⚠ 修正：α=1 不是有限尺寸尖 kink，是随 n 锐化的 crossover**（\|Δslope@α=1\|：n=8→0.099, n=16→0.317, n=20→0.412，随 n 增大）。主张应改为"随 n 锐化的 α=1 crossover（渐近 kink）"，并把有限尺寸锐化作为**附加可证伪预测**（与 2603.24746 的有限尺寸标度语言接轨，但用组合学非拟合）。主线 n 取 16~20。

**P1**：只训到记忆 checkpoint（train→100%、val 仍低，grok 前停）。α∈{0,1,2}，测 L(C_mem,α) 代理量（覆盖的 x mod 2^k 模式数 K(α) + ‖θ‖₂）。乐观(持平)/中性(缓降)/悲观(与 ΔD 同阶降→触发"skew 双重利好记忆"finding）。**无论哪种 C2 都安全**（用实测 λ_c）。

> **P1 v2 已执行（2026-08-21）= 用数据裁决了 M4 协议分叉（§9），并否决了"低 wd 停记忆态"方案**。`experiments/p1v2_memorization_ar.py`，`results/p1v2_memorization_ar.json`。
> - **协议发现（重要）**：子集选择协议下，训练集的进位构成**完全不随 α 变**（实测 c_mean：α=0→0.949, α=2→0.935，理论频率期望应 1→0.33）。原因：无放回抽固定 frac（30%）时，Zipf(2) 也只是选了 x 最小的连续区段，而 trailing-ones 分布与 x 的大小基本无关。**结论：d₀(α) 机制（进位信息饥饿）只在频率偏斜协议下存在——M4/P4 必须用频率加权有放回采样**（这也正是 Zhao "naturalistic skew" 的语义）。K(α) 在固定大小无放回采样下天然无区分度（要记的样本数恒等于 n_train），P1 的理论 proxy 需重新设计。
> - **方法发现**：wd=0.01+init=10 无法停在记忆态——8000 步内多数没到 train 99%（|θ|≈185 几乎不动，大 init 下梯度/正则都推不动）；到达 99% 的 run 其 eval 也同步 99%（n=10 记忆太贵，直接泛化）。**在 n=10 上"纯记忆态"很难构造**；P1 若要测记忆复杂度需回到 n=8 或改协议。
> - ‖θ‖(2)/‖θ‖(0)=1.000：但由于上述原因，这测的不是记忆态，**不能**当作"Δℓ 平坦"的证据。P1 待 P4 之后用频率协议重设计。

> **P1 v1 已执行（2026-08-21）= 暴露前提问题，需 P0.5 先行**。`experiments/p1_memorization.py`，`results/p1_memorization.json`。
> - **理论侧 GO**：K(α) 单调降，K(2)/K(0)=0.472（覆盖模式数减半）→ 组合学侧证实"skew 让记忆需记的更少"。
> - **经验侧测错了态**：wd=0.01 太低、frac=0.5 太多、步数太少 → 模型**直接泛化**（α≤1.5 evalSeq≈1.0），根本没进记忆态。‖θ‖ 几乎不变（比值 0.995）是因为测的是同一个 C_alg，不是"记忆解复杂度与 α 无关"。
> - **根本教训**：P1/P2/P4 全预设 grokking 现象存在，但我跳过了"复现 grokking 曲线"这一步（原执行计划 week1-2）。P0（纯组合学）不受影响，但 P1 依赖 grokking 故露馅。**必须先做 P0.5 复现 grokking，再重做 P1 v2（在延迟窗口内测）。**

**P0.5（新增，前提）**：复现经典 grokking 曲线——train 早到 100%、eval 延迟数千步才跳升。扫 wd∈{0.1,0.3,1.0}×frac∈{0.3,0.4}，n=12，30k 步，记录完整轨迹与延迟 τ。成功判据：存在 τ>500 且 final_eval>0.95 的配置。只有拿到曲线，"记忆态 checkpoint"才有定义。`experiments/p05_grokking.py`。

> **P0.5 v1/v2/v3 全部无 grokking（2026-08-21）→ 根因：任务实现错误**。v1(小init)、v2(大init+norm collapse 187→25)、v3(数据降到 frac=0.05)，模型全部 250 步内直接泛化。**回源核对 Zhao 论文 PDF（papers/zhao_counting.pdf）后确认：**
> - **我实现的是错的任务**。我做的是"并行 bit 分类 / bin(N)→bin(N+1) transduction / little-endian"——而论文 §2.2 **明确点名这个版本是"2-state Mealy machine，平凡"**。这就是三次直接泛化、无 grokking 的根因。
> - **Zhao 真实 LARGECOUNTER**：自回归 next-token 生成，词表{0,1,#}，生成整条 ℒ_n=bin(0)#bin(1)#…#bin(2ⁿ−1)，**big-endian**，decoder-only GPT L=2 H=4 d=64 RoPE，**n=20**。关键难点：**模型必须内部追踪计数器 N，输入不提供当前值**。训练 AdamW lr=1e-3 batch512 **wd=1.0** 最多 5×10⁴ 步，A100 单次 grok~2h，5 seeds，训练集≈30% 空间、按进位链长度分层。
> - **⚠ 重大：Zhao 已做了 H4 的离散版**。§3.2 原文："grokking also occurs under uniform sampling (72.4%, 8/20 strata grokked); stratification **extends** grokking to rare carry chains but **does not create** it."即"carry-balanced 课程拯救稀有进位链 grokking"**已被定性验证**（uniform vs stratified 两个离散点）。**H4 增量必须收窄为：连续偏斜轴 α + 解析临界线 λ_c(α) + 闭式最小剂量 m\*=κN(1−λ/λ_c)。** 这个收窄必须在 novelty statement 里正面处理。
> - **方法论教训**：我跳过了执行计划 week1-2 第一条"Reproduce ACL setup"，臆测任务定义直接往下做。决策（2026-08-21）：**先最小复现 grokking**（自回归+big-endian+无外部当前值这个核心难点，小 n=8~10），确认能 grok 再逐步加到完整设置。

**P2**：植入/取 C_alg（B-RASP）+ 取 C_mem，线性插值 θ_t=(1−t)θ_mem+t·θ_alg 测 loss。有能垒→双阱成立；无能垒→彻底删 H2/spinodal（不影响 C1/C2）；C_alg 植入失败→回复现 Zhao（真警报）。

> **P0.5 v4（2026-08-21）= 正确任务首次 grok 成功**。`experiments/p05v4_ar_grokking.py` + `task_ar.py`（自回归 big-endian，loss 只在 bin(N+1) token 上）。n=8、frac=0.5、wd=0.3、init_scale=5、lr=1e-3：train@200、eval@3600、**τ=3400**、范数 101→18（复杂度坍缩与泛化同步）。图 `figures/p05v4_grokking_curve.png`。但曲线噪声大、振荡。
>
> **P0.5 v5（2026-08-21）= n=8 种子稳定性 FAIL**。5 seeds 只有 1/5 稳定 grok，所有 seeds evMin≈0（eval 从未锁定）。这正是提案自列风险"相界被种子噪声抹平"——真实存在。
>
> **稳定化扫描结论（n=8）**：lr=3e-4+init=2.0 → 3/3 稳定（evMin 0.87–0.91）但**延迟窗口消失**（eval@250，回到立即泛化）；init=3.0 有延迟但不稳定。**n=8 裁定：延迟与种子稳定性互斥——256 个状态的记忆盆地不够宽/深。决策（用户确认）：加大到 n=10~12。**
>
> **P0.5 v6（2026-08-21）= 无延迟，网格方向错误**。`experiments/p05v6_joint.py`（n=10/12 × init 3/4 × lr=5e-4 × betas=(0.9,0.98)，3 seeds/格，30k 步，共 1581s）。四格全部**立即泛化**（eval 在 250 步即过 0.90，tau≈0 甚至为负），且 eval 剧烈振荡（tail_mean>0.9 但 tail_min≈0~0.5）。诊断：v6 相对唯一成功的 v4 配方**同时动了三个旋钮**（lr 1e-3→5e-4、betas→(0.9,0.98)、init 5→3/4），无法归因。**教训：换 n 时先原方照搬，一次只动一个变量。**
>
> **P0.5 v7（2026-08-21）= 延迟随 n 增大而消失；大 init 方向正确但不够**。`experiments/p05v7_transfer.py`，`results/p05v7_transfer.json`。
> - **关键发现 1**：v4 配方原封不动搬到 n=10 → **立即但稳定**泛化（trX=evX=250，tailMin=0.934）。即 n 增大后（参数量不变、状态更多）纯记忆变得更贵，模型跳过记忆盆地直接学算法。v5"n=8 盆地太小"的假设只对了一半：**n≥10 的问题是记忆盆地整个消失，而不是不够深。**
> - **关键发现 2**：init 是最有效的延迟杠杆（Omnigrok 方向确认）：init 5→7→10 使 train crossing 从 250 → 500-750 → 1000-1500 步。n=10 frac=0.3 init=10 出现 tau=1250（seed0），n=12 frac=0.5 init=10 3/3 grok（tail_mean 0.91-0.97）——**但**：tau 普遍 ≈0 或为负（eval 与 train 同步过线），且尾部仍剧烈振荡（tail_min 常近 0）。延迟窗口出现了，稳定性没有。
>
> **P0.5 v8（2026-08-21）= minibatch SGD 显著改善；最佳候选 A=n10/frac.3/wd.3/init10/batch128**。`experiments/p05v8_lockin.py`，`results/p05v8_lockin.json`。
> - **[A] n=10 frac=0.3 wd=0.3 init=10 batch=128：3/3 grok，tau_med=1000**（trX 1250-2000，evX 1250-3000）。离目标只差尾部锁定（tMin 0.06/0.92/0.18，仍振荡）。
> - **[B] wd=1.0（Zhao 的值）在 d=64 小模型上太强**：1/3 grok，范数压到 ~9。Zhao 的超参不可直接移植（他们的 n=20/大语料/batch512 规模不同）。
> - **[D] n=12 frac=0.3：3/3 grok 但 eval 先于 train 过线**（tau≤0）——大 n 下泛化领先记忆，无延迟窗口。
> - **[E] n=8 minibatch 对照**：seed0 出现经典延迟 **tau=2750**（trX=250, evX=3000, tMin=0.664），但 2/3 grok——n=8 种子敏感依旧。
> - 结论：minibatch 是正确方向（对比 v7 全批：tau 与 grok 率同时改善）；剩余唯一问题是**尾部振荡**。
> - **振荡诊断**：细看尾部 log 点不是持续徘徊而是**偶发瞬时崩塌**——绝大多数点 0.95-0.99，偶发单点掉到 0.06-0.2 后立即恢复（疑似罕见 batch 大梯度瞬态踢出）。故 tMin 判据过于苛刻；正确度量 = tail-mean + 崩塌频率（<0.5 点占比）。v9 退火若能消除瞬态则更好。
>
> **P0.5 v9（进行中）**：`experiments/p05v9_anneal.py`——lr 余弦退火能否锁死尾部而不杀延迟。A1（恒定 lr, 60k）vs A2（cosine→0, 60k）vs E2（n=8 + cosine，看经典延迟能否种子稳定）。成功判据：≥4/5 grok 且 tMin>0.85 且 tau_med≥500。输出 `results/p05v9_anneal.json`。
> 注：grokking 配方（大 init + 中 wd）是 Omnigrok 杠杆，非 Zhao 原方（Zhao 用 wd=1.0、无大 init、分层采样）——目前是"可用现象"，尚非"忠实复现"。

**P3**：复用 checkpoint，算 F=D+λℓ，扫 λ 找 F_alg<F_mem 临界点，看是否与 P4 实测 λ_c 同数量级。❌差数量级→MDL 降为纯 motivation 类比，靠组合学内核。

**P4**：最小 2×2，α∈{0,2}×λ∈{low,high}，3 seeds/格。四角有 grok/不 grok 分野且 α=2 边界右移 → 相图落地，放大为正式 M4。噪声抹平→改 grok-probability 序参量 + 重估算力。

> **P4 已执行（2026-08-21，频率协议）= 方向与预期相反，但信号清晰**。`experiments/p4_freq_phase.py`，`results/p4_freq_phase.json`。格点：α∈{0,1,2}×wd∈{0.3,1.0}，A2 配方，3 seeds，序参量 A_unif（均匀全空间）/A_hi（权重前 10%）/A_lo（权重后 50%）。
> - **wd=0.3 下 α=0/1/2 全部 3/3 grok**（A_unif=1.000/1.000/0.918）——α=2 也没困住。原因：60k×128=7.7M 样本让尾部每状态约 9 次曝光，频率饥饿被大采样预算稀释。
> - **但 α=2 出现清晰的尾部滞后梯度**：A_hi=0.998 vs A_lo=0.882（α≤1 时两者都=1.000）。"偏斜伤泛化"的趋势存在且方向与 Zhao 一致。
> - **wd=1.0 不是干净的 λ 杠杆**：α=1 下仍 grok（0.99），α=2 下全崩（A_hi 也只 0.2-0.8，norm→9）——这是 AdamW 大 wd 的数值压垮（与 v8B 一致），不是相变。**λ 轴在我们 wd 范围内未测到 grok/不 grok 分野**。
> - **当前观察到的结构是 α 轴上的连续退化**（A_lo 单调下降、A_hi 保持），非锐利相变。临界 α 应 >2 → P4b 正在细扫 α∈{2.5,3,4}。锐利程度问题（连续退化 vs 相变）与 n 的关系（Zhao n=20 vs 我们 n=10）是正式 M4 的核心设计点。
> - 理论对接：这与预注册 s_n(α) 的"α=1 后快速上升"**定性一致**（α≤1 完全无影响、α=2 开始伤尾部），但 λ_c 的"λ 侧杠杆"未在 wd∈{0.3,1.0} 内显形——需要 wd 中间值（如 0.5/0.7）或把 λ 轴理解为"有效 λ/采样预算"复合量。

> **P4b 已执行（2026-08-21）= 相界找到：α_c∈(2, 2.5) @ wd=0.3**。`experiments/p4b_alpha_scan.py`，`results/p4b_alpha_scan.json`。
> - α=2.5：**干净的 TRAPPED 态**——A_hi≈0.9（头拟合）、A_lo≈0.05（长尾完全没学）、A_unif≈0.26。这正是 Zhao 描述的"被困在启发式解"。
> - α=3：连头部都拟合不良（A_hi 0.6）；α=4：全崩（batch 里几乎只剩 x=0，超出有效信号区）。
> - α 轴结构：1.0 完全 grok → 2.0 尾部滞后 → 2.5 trapped → 3+ 崩坏。

> **P4c 已执行（2026-08-21）= λ 救援失败，方向与 H3 相反 ⚠**。`experiments/p4c_lambda.py`，`results/p4c_lambda.json`。
> - (α=2.5, wd=0.5)：仍被困（A_unif=0.231）；(α=2.5, wd=0.7)：更糟（A_hi 也掉到 0.57）。
> - **(α=2.0, wd=0.5)：从 grok（wd=0.3 时 0.918）退到 partial（0.639）**——加大 wd 把没被困的也拖下水。
> - 结论：当前协议里 wd 与偏斜**同向恶化**（大 wd 压容量/范数，偏斜压有效数据），没有观测到 H3 预言的"λ>λ_c 救援"。C1 的 λ 侧需要重新理解。
> - **P4d（进行中）**区分两个候选解释：(i) λ 窗口在 <0.3（测 wd=0.15）；(ii) 不是稳态相界而是时间问题（α=2.5 @ wd=0.3 跑 120k 步，看是否只是延迟更长——若如此相图应改为延迟图 τ(α)）。

> **P4d 已执行（2026-08-21）= 被困是稳态；λ 方向确认反转**。`experiments/p4d_lambda_dir.py`，`results/p4d_lambda_dir.json`。
> - **120k 步不解救**（α=2.5, wd=0.3：3/3 仍 TRAPPED，A_unif=0.404）——被困不是"延迟更长"，相界是真稳态。
> - **wd=0.15 部分改善被困**（A_unif 0.26→0.47，A_lo 0.05→0.27）但救不回；结合 P4c：α=2.5 下 wd 0.15→0.3→0.5 单调恶化。**λ_c(α) 随 α 递增（H3）被证伪（本协议内）；实测最优 wd 随 α 增大而减小。**
> - 机制解读：wd 与偏斜是**同向的容量/有效信号压缩器**（大 wd 压范数容量，偏斜压有效数据），伤害可加；不存在"加大 λ 压走记忆"的救援通道。理论侧需重推（或改用"有效 λ = wd·∫lr"重新标定），见 §10。
> - **叙事轴心正式移向数据侧救援（H4/m\*）**：P5 正在测最小均匀注入剂量 p*（α=2.5）。

> **P5/P5b 已执行（2026-08-21）= m\* 存在、极小、陡峭**。`experiments/p5_rescue.py`，`results/p5_rescue.json` + `results/p5b_k1.json`。α=2.5, wd=0.3，batch=128 中固定 k 条均匀样本：
> - p=0：TRAPPED（A_unif=0.263）；**p=1/128（k=1，0.8%）：3/3 GROK（A_unif 0.935-0.969）**；p≥1/64：GROK 且 A_unif≥0.975。
> - 剂量响应在 (0, 1/128] 内完成跃迁——极陡。60k×1=6 万条均匀样本 ≈ 每状态 59 次均匀曝光即足以救回。

**决策树关键认识**：走完全树，**没有任何单个 ❌ 杀死选题**——硬内核"实测 λ_c + 组合学 → m\*=κN(1−λ/λ_c)"对 P1/P2/P3 失败全免疫。仅 P0、P4 是真否决点，且都便宜。**选题下限已稳。**

## 8. 实验环境（2026-08-21 锁定）

- 机器：sophgo-System-Product-Name，Ubuntu 24.04，kernel 7.0。
- GPU：**RTX 5080 16GB（Blackwell, sm_120）**，驱动 580.173，CUDA 13.0 运行时 / nvcc 12.8。
- **关键约束**：sm_120 必须用 **PyTorch cu128** wheel（`--index-url https://download.pytorch.org/whl/cu128`）；cu121/cu124 会报 "no kernel image available"。
- 隔离环境：`/home/nsc/coding/grokking_research/.venv`（uv venv, Python 3.12.12）。**后续所有实验都用它。**
- 激活：`source .venv/bin/activate` 或直接 `.venv/bin/python`。
- 详见 `SETUP.md`。

---

## 10. 先导实验综合判定（2026-08-21/22，P0→P9 全链完成 = **总裁定 GO**）

### 10.1 结果全景

| 先导 | 问题 | 结果 | 产物 |
|---|---|---|---|
| P0 | 进位链组合学解析 | GO（闭式精确到机器精度）| `p0_combinatorics.py` |
| P0.5 v4-v9 | 可靠 grokking testbed | **锁定 A2 配方**（n=10, frac.3, wd.3, init10, batch128, cosine60k：5/5 grok, 4/5 锁定）| `p05v4-v9` 系列 |
| 理论预注册 | 冻结对照曲线 | s_n(α) 无参形状 + n-sharpening + 竞争形状 H(α) | `theory_curves.py` |
| P1v2 | Δℓ(α) 平坦性 / 协议 | **裁决协议分叉：必须频率偏斜**；"纯记忆态"在 n=10 不可构造 | `p1v2_memorization_ar.py` |
| P4/P4b | α 轴相界 | **α_c∈(2,2.25)@wd=0.3**；α=2.5 干净稳态 TRAPPED（A_hi≈0.9, A_lo≈0.05）| `p4_freq_phase.py`, `p4b_alpha_scan.py` |
| P4c/P4d | λ 侧救援 | **λ 轴方向与 H3 相反**（wd 越大越糟；最优 wd 随 α 递减；120k 步不逃逸=真稳态）| `p4c_lambda.py`, `p4d_lambda_dir.py` |
| P5/P5b | 数据侧救援 m* | **m* 存在且极小：每 batch 1 条均匀样本（p≈0.8%）即 3/3 救回；剂量响应陡峭**（0 与 1/128 之间跃变）| `p5_rescue.py` |
| P6/P6b | m*(α) 曲线 | **m* 平坦：α∈[2.25,3.0] 全部 ≤1/128**（含 head 饥饿的 α=3.0）——增长预测被证伪，换来"0.8% 均匀注入救任意偏斜"的普适结论 | `p6_mstar_alpha.py`, `p6b_mstar_a300.py` |
| P7b/P7 | τ(α) 慢化 | **τ: 2.25k@α=0 → 1.25k@α=1 → 49.5k@α=2 → >240k@α=2.5**（240k 实测仍 2/3 trapped）；**wd=0.05@α=2.5 直接 3/3 GROK** | `p7b_tau_alpha.py`, `p7_slowing.py` |
| P8 | **(α, wd) 相图** | **完成（126 runs，8-21 23:45）= Figure 1 成立**：右侧偏斜 trap/饥饿区（α≥3 连 wd=0.05 都不 grok）+ 上侧 CRUSH 区（随 α 向低 wd 蔓延）；grok 通道 wd 上界随 α 单调递减。热图 `figures/p8_phase_diagram.png` | `p8_phase_diagram.py`, `p8c_plot.py` |
| P9 | 边界形状 vs 预注册候选 | **完成（8-22）= 无干净单标度律**：ceiling(α) 实测 0.313@2 / 0.115@2.25 / 0.085@2.5；四候选拟合最大相对误差 **H_tail10=0.68（最接近）** > H_lo=0.83 > E_c=1.38 > 2^(−α)=1.61。**质量家族方向性胜出（支持 §11.2 λ_c=H_tail/Δℓ），d₀/E_c 家族形状被排除**；但最好候选也差 ~2×，不足以定标度律（3-4 个解析点 + 粗 wd 网格 + 种子噪声，α=2 行非单调）| 8-22 会话内分析（`results/p8_phase_diagram.json` × `results/p9_theory_boundary.json`）|
| P10 | 细扫边界（n=10） | **完成（8-22，136 runs）**：精修 ceiling 2.0→0.305 / 2.1→0.167 / 2.25→0.124 / 2.5→0.086；α=2 行凹陷确认为种子噪声（补 seeds 后 0.15→4/5、0.20→4/5）；**α=3 在所有 wd 低至 0.02 均不 grok（均值 0.44-0.64）→ 右边界近似垂直，饥饿不可被降 wd 拯救**；⚠ 单标度排名翻转：精修后 E_c（最大误差 0.39）与 2^(−α)（0.45）反超 H_tail10（0.59）——家族判别在当前分辨率下不稳健 | `p10_fine_boundary.py`, `p10b_gap_fill.py`（补 α=2.4/2.6 天花板 + 2.1 陡降，运行中）|
| P11 | n=12 全网格（有限尺寸） | **完成（8-22，126 runs）= 相图在 n=12 复现，grok 区整体内移**：ceiling(2) 0.305→0.211（比值 1.44）、ceiling(2.25) 0.124→0.109（1.14）；右边界 @wd=0.05 从 α∈(2.75,2.9) 左移到 (2.5,2.75)。**非 n=10 伪影**。理论判别：质量家族预言 n=10→12 下移 ~4×、进位链家族预言 ~1×，实测 1.44× 介于其间——P11b 预算对照裁定见 §10.5 | `p11_n12_grid.py`, `p11b_budget_control.py` |
| P11b | 预算对照（动力学 vs 热力学） | **完成（8-22，10 runs × 240k 步）= 混合裁定，以动力学为主 + 热力学残余**：4× 预算（补齐每状态曝光）下 5 格无一恢复 GROK（全部 PARTIAL）；但 (2.75,0.05) 均值 0.524→0.786 显著爬升（边界附近临界慢化仍在）、(2.0,0.2)(2.25,0.15) 与 n=10@60k 曝光匹配后基本持平，(2.0,0.3)(2.5,0.1) 仍差 → 1.44× 的内移**大部分是每状态曝光（动力学）效应，热力学残余真实存在**（ceiling(2) 曝光匹配后仍 ~1.2× 下移，靠近进位链家族预言的 ~1×，远离质量家族的 4×）| `p11b_budget_control.py` |

### 10.2 四大发现（相对原提案的修正）

1. **协议发现**：d₀(α) 机制（进位信息饥饿）只在**频率偏斜**下存在；无放回子集选择下训练集进位构成与 α 无关。M4/M5 必须用频率协议（已实现 `zipf_batch_sampler`）。这也正是 Zhao "naturalistic skew" 的本意。
2. **负结果（重要且诚实）**：**H3 的 λ_c(α) 方向被证伪**（提案预言递增，实测递减）：加大 wd 不能救被困态，反而同向恶化；α=2.5@wd=0.3 是真渐近 trap（240k 步不逃逸）。
3. **正结果一（相图复活）**：wd 轴在 α=2.5 处**单调**（0.05→grok，0.15→partial，0.3→trap，0.5/0.7→更糟）——grok 通道的 wd 上界随 α 递减，且高 wd 是第二个失败区（过度正则化压垮）。**(α, λ) 相图以修正后的方向复活**（§11.2 的 λ_c(α)=H_tail(α)/Δℓ 递减线，P8 正在实测整条边界）。
4. **正结果二（数据侧普适救援）**：**m* 平坦且 ≤1/128 对所有 α∈[2.25,3.0] 成立**，包括 head 已饥饿的 α=3.0；trap 与饥饿两种失败模式有同一个数据侧解。机制：均匀注入抬高有效分布尾部质量（改变热力学），每均匀样本的信息量与 α 无关 → 阈值与 α 无关。

### 10.3 修订后的最小可行主张（P6-P8 后再升级）

> 进位链组合学（P0/Lemma A）→ **(α, wd) grokkability 相图**（两条失败边界：右侧偏斜 trap/饥饿区 + 上侧过度正则化压垮区；grok 通道的 wd 上界随 α 递减，方向与提案相反、可测、有理论）→ **数据侧最小救援剂量 m*≤1/128 且与 α 无关**（陡峭剂量响应，同时解两种失败模式）→ 临界慢化 τ(α)（49.5k@2 → >240k@2.5）把"相界"与"预算"连起来。

叙事：从"weight decay 是描述长度场"（λ 为主角）转向"**偏斜的数据流与正则强度竞争稀有特征：数据决定哪些特征可被巩固，wd 决定哪些特征被修剪——grokkability 由两者的比值决定**"。三个实测结构（相图边界、m* 平坦、τ 发散）互相咬合，且全部回应 Zhao 的开放局限。

### 10.4 投稿前待办

1. ~~P8 收尾~~（**完成 8-22**：热图已出，wd-ceiling 边界已提取，见 P9 行）。
2. ~~边界形状 vs 理论~~（**完成 8-22，结果与预注册锚点预期相反**：预检最看好的 E_c/exp2 被数据排除——实测 ceiling 衰减远快于二者（log-log 斜率 vs α 达 −5.9，而 E_c/exp2 家族只支撑 ~−1.6）；最接近的是 H_tail10（质量家族）但仍有 ~2× 偏差。**解读：λ_c=H_tail/Δℓ 方向正确但非纯单标度**，边界精确形状是当前最弱一环。补救路径：(a) 边界附近细扫 wd（当前网格粗、α=2 行非单调、仅 3-4 个解析点）；(b) n=12 复测看形状是否随 n 收敛；(c) 接受"现象学边界 + 定性判别质量家族>d₀家族"的表述，把标度律留给正式实验。
3. **m* 平坦的机制（已可定量）**：均匀注入给每个状态每 batch 下界概率 p/2^n = (1/128)/1024 ≈ 7.6e-6。关键对照：最长进位态 x=2^n−1 在 Zipf(α=2.25) 下的自然概率仅 ≈8e-8（α 更大更小）——**注入把它抬高 ~100 倍**，而总尾部质量 H_lo(α=2.25)=1.3e-4 反而比注入底板大 5 倍。说明决定性变量不是尾部总质量，而是**最长进位态的逐态覆盖**（学完整算法的瓶颈状态）：Zipf 对高 x 的压制在 α≥2 后已使最长链态指数饥饿（与总质量无关），而均匀注入恰好 α 无关地恢复它们——这就是 m* 平坦的来源。自然推论：救援剂量针对的是"每状态最小覆盖"而非"信息总量"，与 d₀（总量）观点可判别。

### 10.5 P10/P11 后的边界形状与有限尺寸裁定（2026-08-22，进行中）

**已确认：**
1. **右边界近似垂直**：α=3 在 wd 低至 0.02 全不 grok（P10），α∈(2.9,3) 处饥饿区不可被任何 wd 拯救——"(α, wd) 平面存在 wd 无法穿越的垂直失败壁"是相图的结构性特征，写进 Figure 1。
2. **相图非 n=10 伪影**（P11）：n=12 复现同构相图，grok 区随 n 内移，方向与 n-sharpening 一致。
3. **α=2 行凹陷 = 种子噪声**（P10 补 seeds 修复）。

**未决（家族判别不稳健）：** P9 粗拟合说质量家族胜出，P10 精修后排名翻转到 E_c/2^(−α) 领先——两次裁定互相矛盾说明当前数据（4 个解析天花板点）不足以判别形状家族。正在补：P10b（α=2.4/2.6 天花板 + 2.1 陡降钉死，~48 runs）。判别的另一钥匙在 α≤1 区段（E_c 与 2^(−α) 差 18% vs 50%）——但该区 ceiling >0.5，需要 wd>0.5 扫描且与 CRUSH 区重叠，是正式实验 M4 的设计点。

**P10b 完成（8-22，48 runs）→ 家族判别落定（6 点拟合）：** 合并 P8+P10+P10b 后解析天花板 6 个：**0.306@2.0 / 0.184@2.1 / 0.124@2.25 / 0.110@2.4 / 0.086@2.5 / 0.069@2.6**；α=2.75 边缘（wd=0.03 时 3/5 grok、均值 0.892 贴线），α≥2.9 全灭 → **n=10 右边界定在 α≈2.7**。单标度拟合（6 点）：**E_c 最好（最大误差 0.46）> 2^(−α)（0.53）> H_tail10（0.95）> H_lo（1.78）**——与 P10 的 4 点拟合排名一致、与 P9 粗网格排名相反。**结论：P9 的"质量家族胜出"是粗网格伪影，正式裁定为进位链/指数家族方向占优；但 E_c 拟合斜率 2.59（完美单标度应为 1.0）、实测衰减 log-log 斜率 −5.17 比所有候选都陡——没有任何现成候选是干净的标度律**。边界形状的最终表述："实测边界 + 四候选判别（进位链 > 指数 > 质量）+ 陡于全部候选"——判别性结果本身可写，精确标度律留给正式实验（或推新理论）。

**P11b 完成（8-22，预算对照）→ 内移机制 = 动力学为主 + 热力学残余：** n=12 五个失败/边缘格跑 240k 步（= 补齐每状态曝光 4×），**无一恢复 GROK**（全部 PARTIAL，A_hi 仍 0.95-1.0 → 是 trap 结构非 crush）。逐格看：曝光匹配后 (2.0,0.2)(2.25,0.15)(2.75,0.05) 与 n=10@60k 大体持平（其中 (2.75,0.05) 从 0.524 爬到 0.786——边界附近临界慢化在 n=12 同样存在），但 (2.0,0.3)(2.5,0.1) 仍显著差于 n=10。**裁定：ceiling(2) 的 1.44× 内移大部分是曝光/动力学效应，热力学残余 ~1.2×；曝光匹配后的有限尺寸比值靠近进位链家族（~1×）、远离质量家族（4×），与 P10b 的家族判别一致。**方法论教训（正式 M4 必须遵守）：**边界测量必须曝光匹配（步数 ∝ 2^n）或同时报预算曲线**，否则把动力学读成热力学。

**先导链（P0→P11b）至此全部完成。总裁定维持 GO**：相图（两条失败边界 + 垂直饥饿壁）+ m* 平坦救援 + τ 慢化 + 有限尺寸方向，全部落定；边界精确标度律与 α≤1 区段（需 wd>0.5，与 CRUSH 重叠）是正式实验的设计点。下一步 = 正式实验战役规划（M4 曝光匹配协议 / M5 / M6）。
**未决（有限尺寸的机制）：** ceiling(2) 比值 1.44 介于质量家族（~4×）与进位链家族（~1×）之间，但可能是预算混杂（n=12 状态多 4×、每状态曝光少 4×）。P11b（n=12 失败格 × 240k 步预算对照）裁定：grok → 动力学成分；仍 trapped → 热力学 n-sharpening。
4. **n=12 复测**关键格点（有限尺寸方向：相界应随 n 移动，理论 s_n 的 n-sharpening 有预言）。
5. P2（双阱插值）、P3（自由能序）在频率协议下重定位。
6. ~~补引用~~（§5.7 已完成：Omnigrok=2210.01117 ✓，Žunkovič=2210.15435 ✓，Hinton&van Camp=NeurIPS'93 proceedings，"Sharkey 2024"查无此文→改引 arXiv:2501.14926 Braun et al. incl. L. Sharkey 并核其是否支持原表述）。

---

## 11. 理论修正：热力学/动力学分解（2026-08-21，λ 方向重推）

### 11.1 发现提案 T3 的 ΔD 方向错误

原 T3：ΔD(α) ∝ N(E₀[c]−E_α[c])——"每样本进位信息赤字"，随 α **递增**。
但自由能 F = D_α + λℓ 里正确的 ΔD 应为**记忆解在评估分布下的数据惩罚**：

> ΔD(α) = D_α(C_mem) − D_α(C_alg) ≈ 尾部概率质量 H_tail(α)（记忆只盖头部，惩罚全部来自未覆盖的尾部）

而 H_tail(α) 随 α **递减**（§9 的 H(α)：0.5→0.005 @ α=1.5, n=12）。**两个"ΔD"方向相反**：信息赤字（每样本语义）递增 vs 数据惩罚（种群质量语义）递减。

### 11.2 修正后的自由能 → λ_c(α) 递减，与实测一致

λ_c(α) = ΔD(α)/Δℓ **递减**——即偏斜下算法解在更小的 λ 就已是自由能极小。这正好解释 P4c/P4d 实测的"最优 wd 随 α 递减"（α=2 最优 ~0.3，α=2.5 最优 ≤0.15）。**"λ 轴反转"不是反常，是提案方向写反了。**

### 11.3 但热力学 λ_c 递减与"α 大更难 grok"并存 → 必须引入动力学

若只有热力学，λ_c 递减意味着 α↑ 更容易 grok——与实测相反。矛盾化解：**被困是动力学亚稳，不是热力学平衡**。偏斜数据下头部梯度主导，SGD 先落入并滞留记忆盆地；λ_c(α) 只是"终点谁赢"的判据，"何时到达"由逃离时间 τ(α, λ, p) 决定，τ 随 α 急剧增长（临界慢化）。任何有限预算 B 下存在有效相界 α_c(B)（§10 的 α_c 随预算右移：t≤40k 时 α_c=1，t=60k 时 α_c=2）。

### 11.4 统一框架（三个实测现象全部自洽）

| 实测现象 | 修正后解释 |
|---|---|
| λ 方向反转（最优 wd 随 α 递减）| 热力学 λ_c(α)=H_tail(α)/Δℓ 递减 + wd 的容量副作用 |
| α_c 随预算右移 | 动力学逃离时间 τ(α) 发散（临界慢化），α_c(B) 是预算交点 |
| m\* 小且陡峭 | 均匀注入直接给尾部梯度 + 长进位信号，指数压低 τ |

### 11.5 新的可证伪预测（待验证）

1. **τ(α) 慢化**：α=2.5 在 240k 步（T_max 同步放宽）应完成 grokking（P7 待跑）；τ(α) 在 α∈[2, 2.5] 急剧增长（可能数十倍/单位 α）。
2. **α_c(B) 标度**：预算 B 加倍，α_c 右移量应可从 τ(α) 曲线预测。
3. **m\*(α, B)**：m\* 应随 α 增长、随 B 增长而减小（P6 在测 α 依赖）。
4. **wd 非单调窗口**：α=2.5 下 wd→0.05 应先升后降（无驱动力），最优 wd(α) 曲线递减且有下界。

### 11.6 预测验证进度（2026-08-21 晚）

**预测 1（τ 慢化）——慢化成立，但"240k 内完成"的具体预测被证伪（P7b + P7）**：

τ(α) 零成本提取（`experiments/p7b_tau_alpha.py`，wd=0.3, A2 recipe, n=10, 3 seeds，首次持续 A_unif>0.9 的步数）：

| α | τ_med | 备注 |
|---|---|---|
| 0 | 2,250 | |
| 1 | 1,250 | |
| 2 | **49,500** | 逼近 60k 预算，本身可能预算受限 |
| 2.5 | **>240,000** | P7 实测 |
| 3, 4 | 永不 | head 也失败（饥饿区） |

P7（`experiments/p7_slowing.py`，α=2.5, wd=0.3, 240k 步，T_max=240k，anneal 不提前冻结）：**3 seeds 依然 2 TRAPPED + 1 partial（A_unif=0.433/0.559/0.321）**。从 120k（A_unif≈0.5 平台）到 240k 无实质爬升——**修正之前"仍在爬升、是 anneal 冻结 artifact"的解读**：α=2.5@wd=0.3 是真实的渐近 trap（或 τ 发散至 >240k）。慢化结论保留（α=1→2 跳 40×，α=2→2.5 至少再 5×），但 §11.5 预测 1 的"240k 应完成"被证伪：**α_c(wd=0.3) 不是纯预算交点，右侧边界更可能是真渐近边界（临界慢化 + 渐近 trap 的组合）**。

**预测 3（m\* 随 α 增长）——已证伪，取而代之的是更强结论：m\* 平坦且与 α 无关（P6 + P6b）**：`experiments/p6_mstar_alpha.py` + `experiments/p6b_mstar_a300.py`（wd=0.3, 60k, 3 seeds）：

| α | p=0 | p=1/128 | p=1/32 | p=1/8 | p=1/2 |
|---|---|---|---|---|---|
| 2.25 | 0/3（2 trapped + 1 partial, A_unif=0.565）| **3/3 GROK** | — | — | — |
| 2.5 | 0/3（steady trap, A_unif=0.26）| **3/3 GROK**（P5）| 3/3（P5）| 3/3（P5）| 3/3（P5）|
| 2.75 | 0/3（A_unif=0.158, A_hi≈0.78 混合态）| **3/3 GROK** | 3/3 | 3/3 | — |
| 3.0 | 0/3（P4b：head 饥饿 A_hi=0.6）| **3/3 GROK**（P6b）| 3/3（P6b）| 3/3 | 3/3 |

- **α_c 进一步收紧到 (2, 2.25)**（α=2.25 p=0 已 0/3）。
- **m\* 在整个 α∈[2.25, 3.0] 测量区间平坦于 ≤1/128**——"每 batch 一份均匀样本（0.8%）即可拯救任意偏斜度，包括 head 已饥饿的 α=3.0"。T4 增长方向的预测错误；正确机制（§11 框架内）：**均匀注入改变的是热力学本身**——把有效分布的尾部质量从坍缩拉回非零（λ_c^eff 随 p 下降到当前 λ 之下），而不是以剂量累积方式"喂信息"。均匀样本的每样本信息与 α 无关，所以阈值也与 α 无关。剂量响应在 (0, 1/128] 内跳变（batch 量化下界）。
- **失败模式谱系**：α=2.5 是纯 trap（A_hi≈0.9, A_lo≈0.05）；α=2.75 是混合（A_hi≈0.78）；α≥3 是饥饿（head 也塌）。且饥饿态同样被 1/128 拯救——"被困于启发式"和"信号饥饿"两种失败模式有**同一个数据侧解**，这是论文可以直接主张的工程结论（对 Zhao 开放限制的完整回答）。

**预测 4（wd 非单调窗口）——被证伪，代之以 wd 轴单调 + 二维相图复活（P7）**：α=2.5 下 **wd=0.05 → 3/3 GROK（A_unif 0.977/0.988/0.998）**，与 wd=0.15 partial、wd=0.3 trap、wd=0.5/0.7 更糟合成**单调递减的 wd 轴**：α=2.5 的 wd 阈值在 (0.05, 0.15)。这直接验证 §11.2 的热力学 λ_c(α) 递减——**grok 通道的 wd 上界随 α 递减**，且高 wd 区是第二个失败模式（过度正则化/压垮，P4 的 wd=1.0）。

**推论：(α, wd) 相图是本论文的 Figure 1**。提案 H3 的相图目标以修正后的形式复活：临界线方向与提案相反（λ_c 递减而非递增），但可测、可重复、有理论（§11.2 的 H_tail(α)/Δℓ）。**P8（`experiments/p8_phase_diagram.py`）已启动**：α∈{0,1,2,2.25,2.5,2.75,3} × wd∈{0.05,0.1,0.15,0.2,0.3,0.5}，3 seeds，126 runs（~3.3h），四分类 verdict（GROK/TRAP/CRUSH/PARTIAL），输出 grok-count 矩阵与热图。

---

## 9. 理论曲线预注册（2026-08-21，任何偏斜训练实验之前冻结）

`experiments/theory_curves.py` → `results/theory_curves.json` + `figures/theory_curves.png`。目的：α 扫描（M4）的对照曲线先于实验冻结，避免事后拟合。

**新精确结果**：
- **E₀[c] = 1 − 2^(−n) 精确**（几何尾在 c=n 截断）。由此 d₀(0)=0，归一化必须除以 E₀[c] 而非 d₀(0)。
- **λ_c(0)=0 是 ansatz 的合法推论**：均匀数据下记忆无数据优势，任意 λ>0 都应 grok（方向上与 Zhao"uniform 72.4% 分层能 grok"一致）。拯救剂量 m\*(0,λ)=0（均匀无需拯救），m\* 公式改以单一尺度 Λ 参数化：m\*/(κN)=max(0, 1−(λ/Λ)/s_n(α))。

**预注册的形状（s_n(α)=d₀(α)/E₀[c]，无自由参数）**：α≤0.75 近平坦（n=12 时 ≤0.056），α=1 附近急升，α>1 快增，饱和值 2/3。n-sharpening 单调：**|Δslope@α=1|：n=8→0.099, 10→0.154, 12→0.210, 16→0.317, 20→0.412**（补齐了 testbed 区段 n=10/12）。

**⚠ 发现一个必须在 M4 设计时处理的竞争形状**：H(α)=偏斜子集选择后的 held-out 种群质量，随 α 坍缩得比 s_n 快得多（n=12：0.5→0.078@α=1→0.005@α=1.5）。两种观点**方向一致**（都预言"偏斜困住模型"）但**形状不同**：d₀ 观点（进位信息饥饿，凹升）vs H 观点（held-out 质量坍缩，凸降）——α 扫描实验可以判别，这本身是加分的可证伪结构。

**⚠ M4 的协议设计分叉（已由 P1v2 数据裁决 → 频率偏斜）**：
1. ~~子集选择~~（`zipf_split_train`）：P1v2 实测训练集进位构成不随 α 变（c_mean 平坦）→ **d₀ 机制在此协议下不存在**，只能作为负对照。
2. **频率偏斜**（胜出）：训练池 = 全空间，minibatch 按 (x+1)^(−α) 加权有放回采样（`task_ar.zipf_batch_sampler` 已实现）；eval = 均匀全空间 acc（A_unif）+ 高频头/低频尾分区 acc（A_hi/A_lo）。"被困" = A_hi 高而 A_unif 低。
首个实验：`experiments/p4_freq_phase.py`（2×2+对照：α∈{1,2}×wd∈{0.3,1.0} + α=0 对照），运行中。
